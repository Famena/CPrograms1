#include <stdio.h>
int main()
{
    int number;

    printf("INPUT: ");
    scanf("%d", &number);

    if(number % 2 == 0)
        printf("Even");
    else
        printf("Odd");
    else
        if(number<0);
    printf("invalid");

    return 0;
}
